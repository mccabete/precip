[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "precip_helper"
version = "0.1.0"
description = "Helper functions for ingesting and cleanig precipitation and fire data."